import { CityprefixPipe } from './cityprefix.pipe';

describe('CityprefixPipe', () => {
  it('create an instance', () => {
    const pipe = new CityprefixPipe();
    expect(pipe).toBeTruthy();
  });
});
